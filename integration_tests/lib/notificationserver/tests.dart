part of or_test_fw;

/**
 * TODO: Add tests for both broadcast, send and FIFO message ordering.
 */
void runNotificatioTests() {
  group('NotificationService', () {

    setUp (() {
    });

    tearDown (() {
    });

  });
}
