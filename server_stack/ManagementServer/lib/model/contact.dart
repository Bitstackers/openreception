part of adaheads_server_model;

class Contact {
  int id;
  String fullName;
  String contactType;
  bool enabled;
  
  Contact(int this.id, String this.fullName, String this.contactType, bool this.enabled);
}
